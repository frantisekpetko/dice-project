{
  "name": "dice-project",
  "version": "1.0.0",
  "description": "POC - NodeJS REST API using SQLite as a database.",
  "scripts": {
    "postinstall": "cd client && npm install && cd .. && npm install",
    "build": "cd client && node scripts/build.js"
  },
  "dependencies": {
    "bluebird": "*",
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "ejs": "*",
    "express": "*",
    "helmet": "^3.13.0",
    "morgan": "^1.9.1",
    "sqlite": "*"
  }
}
